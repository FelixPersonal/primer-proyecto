const { Router } = require('express');
const route = Router();
const verificarToken = require('../middlewares/verificarToken');

const {  } = require('../controllers/abonos');

module.exports = route;